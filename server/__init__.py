"""HTTP face of the Spiyweb library: the browser UI's backend.

Not part of the installable package - `server/` and `web/` live in the
repository the same way `ui/` does, behind the optional `spiyweb[web]` extra.
The library itself keeps `dependencies = []`.
"""
